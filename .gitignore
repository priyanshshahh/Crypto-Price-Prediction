# Git ignore — keep the public OSS repo lean

node_modules/
.env
dist/
.venv/
*.pyc
__pycache__/
.pytest_cache/

# Raw data cache (re-fetched by pipeline; fixture in tests/fixtures/)
/data/
.vercel
.env*
!.env.example

# Internal notes, competitive research, interview study (not for GitHub)
/private/

# Legacy exploratory artifacts (not part of the maintained product)
create_dashboard.py
cryp_*.ipynb
crypt_*.ipynb
.vite/
