# Pinned from the verified run environment (Python 3.12, macOS arm64).
# TensorFlow currently requires Python <= 3.12 — create the venv with python3.12.
pandas==3.0.3
numpy==2.5.1
matplotlib==3.11.0
scikit-learn==1.9.0
statsmodels==0.14.6
requests==2.34.2
tensorflow==2.21.0
pytest==9.1.1
